import 'package:flutter/rendering.dart';

class AppColor {
  AppColor._(); // prevent instantiation
  static const Color primary = Color(0xFF212121);
  static const Color secondary = Color(0xFF373737);
}
